---
import Search from './Search';
import { getCollection } from 'astro:content';
const posts = (await getCollection('blog')).filter(
	(post) => (
        post.data.draft?.valueOf() !== true
            && post.slug
    )
);
---

<Search client:load searchList={posts} />